{
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^0.16.3"
  },
  "name": "JSON_SERVER_DEBTS",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon main.js",
    "start": "yarn run generate-data && node main.js",
    "generate-data": "node generate-data.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.9"
  }
}
